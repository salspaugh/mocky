{
    "columns": ["FlightDate"],
    "transformations": [
        {
            "count": "*"
        }
    ],
    "query": "select FlightDate, count(*) from faa group by FlightDate order by FlightDate;"
}
