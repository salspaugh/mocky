{
    "columns": ["UniqueCarrier", "CarrierDelay", "WeatherDelay", "NASDelay", "SecurityDelay", "LateAircraftDelay"],
    "transformations": [
        {
            "avg": "CarrierDelay",
            "avg": "WeatherDelay",
            "avg": "NASDelay",
            "avg": "SecurityDelay",
            "avg": "LateAircraftDelay",
            "sum": ""
        }
    ],
    "query": "select UniqueCarrier, avg(CarrierDelay), avg(WeatherDelay), avg(NASDelay), avg(SecurityDelay), avg(LateAircraftDelay), avg(CarrierDelay) + avg(WeatherDelay) + avg(NASDelay) + avg(SecurityDelay) + avg(LateAircraftDelay) as a from faa group by UniqueCarrier order by a;"
}


