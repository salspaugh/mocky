{
    "columns": ["OriginCityName", "DestCityName"],
    "transformations": [
        {
            "count": "*"
        }
    ],
    "query": "select a, b, count(*) as c from (select OriginCityName as a, DestCityName as b from faa where OriginCityName > DestCityName union all select DestCityName as a, OriginCityName as b from faa where OriginCityName <= DestCityName) group by a, b order by c desc limit 25;"
}
